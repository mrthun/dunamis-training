<% %w(Personal_Data Skills Preferences Pays Credentials Locations).each do |link| %>
  <% next if (link == "Pays" && !current_user.is_organization? ) %>
  <%= link_to link, profile_registrants_path(:r_id => @registrant.id, :link => link.downcase ), :class=>"profile_links" %> |
<%end%>